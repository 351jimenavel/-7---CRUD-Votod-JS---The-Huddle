<!-- Recibir temas ya ordenados desde el Controlador.

Pintar título, votos y un botón “Votar” (con data-id para el JS).

Link a “Nuevo tema”. -->

<!DOCTYPE html>
<html>
<head>
    <title>Ejemplo EJS</title>
</head>
<body>
    <h1>Lista de temas</h1>

    <% if (error) { %>
        <p style="color:red"><%= error %></p>
    <% } %>

    <% if (temas && temas.length) { %>
        <ul>
            <% temas.forEach((t) => { %>
            <li>
                <strong><%= t.titulo %></strong> — Votos: <%= t.votos %>
                <form method="POST" action="/temas/<%= t.id %>/votar" style="display:inline;">
                <button type="submit">Votar</button>
                </form>
            </li>
            <% }) %>
        </ul>
    <% } else { %>
        <p>No hay temas aún.</p>
    <% } %>

    <p><a href="/temas/nuevo">Crear nuevo tema</a></p>

</body>
</html>